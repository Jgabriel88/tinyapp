<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>

    <% if(user) {%>
      <span class="mx-3 text-light">Logged in as: <%=user.email%></span>

      <form method="POST" action="/logout" class="text-light">
        <button class="btn btn-danger px-2">Log out</button>
      </form>
      <% } if(!user) {%>

        <button class="btn btn-info mx-1 px-2 "> <a class="text-light" href="/login">Sign in</a></button>
        <button class="btn btn-info mx-1 px-2"> <a class="text-light" href="/register">Register</a></button>

        <% } %>

  </nav>
</header>