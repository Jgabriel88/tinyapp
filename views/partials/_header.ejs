<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>

    <% if(username) {%>
      <span class="mx-3 text-light">Logged in as: <%=username%></span>

      <form method="POST" action="/logout" class="text-light">
        <button class="btn btn-danger px-2">Log out</button>
      </form>
      <% } if(!username) {%>
        <form method="POST" action="/login" class="text-light">
          <input type="text" name="username">
          <button class="btn btn-danger px-2">Sign in</button>
        </form>
        <% } %>

  </nav>
</header>